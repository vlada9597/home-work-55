Homework #55 — Асинхронна робота з файлами у Node.js


📋 Опис завдання

Метою роботи є створення трьох асинхронних функцій для роботи з файлами за допомогою модуля fs/promises у Node.js:

writeFileAsync(filename, content) — записує дані у файл.

readFileAsync(filename) — читає вміст файлу.

deleteFileAsync(filename) — видаляє файл.

Кожна функція реалізує асинхронну логіку з використанням async/await, має логування результатів роботи та обробку можливих помилок.

--------------

🧩 Функціональні вимоги

1. writeFileAsync(filename, content)

Приймає два параметри:

filename — ім’я файлу для запису

content — текстовий вміст

Виконує асинхронний запис за допомогою fs/promises.writeFile.

У разі успіху виводить:

Файл успішно записано


У разі помилки:

Помилка при записі файлу: <опис>

--------------------

2. readFileAsync(filename)

Приймає один параметр — ім’я файлу.

Виконує асинхронне читання через fs/promises.readFile.

У разі успіху:

Файл успішно прочитано: <вміст>


Якщо файл не існує:

Файл не існує: <ім’я файлу>


Інші помилки:

Помилка при читанні файлу: <опис>

--------------

3. deleteFileAsync(filename)

Приймає ім’я файлу.

Видаляє файл через fs/promises.unlink.

Успішне видалення:

Файл успішно видалено


Якщо файл не існує:

Файл не існує: <ім’я файлу>


Інші помилки:

Помилка при видаленні файлу: <опис>




⚙️ Технології

Node.js (версія 18+)

Модуль fs/promises

Синтаксис ES6+

Асинхронність через async/await




🚀 Приклад використання
import { writeFileAsync, readFileAsync, deleteFileAsync } from './main.js'

// Запис файлу
await writeFileAsync('example.txt', 'Привіт, це тестовий файл!')

// Читання файлу
const content = await readFileAsync('example.txt')
console.log('Прочитаний вміст:', content)

// Видалення файлу
await deleteFileAsync('example.txt')

🧾 Очікуваний результат у консолі
#54. JavaScript homework example file
Файл успішно записано
Файл успішно прочитано: Привіт, це тестовий файл!
Прочитаний вміст: Привіт, це тестовий файл!
Файл успішно записано
Файл успішно видалено

🧠 Обробка помилок

Усі функції містять блоки try...catch:

помилки логуються через console.error();

якщо файл не існує, виводиться спеціальне повідомлення "Файл не існує".


🧩 Тестування

Функції підготовлені для подальшої інтеграції з JEST — можна мокати методи з fs/promises і перевіряти поведінку при різних сценаріях (успішний запис, відсутній файл, помилка доступу тощо).

📦 Запуск проєкту

Створи файл main.js і встав код.


Запусти у терміналі:

node main.js

